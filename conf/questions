Dans quel fichier je peux changer mon prompt de manière permanente ?
Dans quel fichier se trouve la liste des utilisateurs du système ?
Quels sont les différents champs dans le fichier contenant la liste des groupes ?
Combien de champs y a t-il par user dans le fichier contenant la liste des users ?
A quoi correspond le ' :x: ' dans le fichier contenant la liste des users ?
Quelle est la commande qui permet de se connecter en tant que root ?
Je veux créer l'utilisateur bob, sachant que bob préfère utiliser Python, comment faire ?
Comment mettre à jour ses paquets (2 commandes) ?
Je veux qu'une vache me dise "hello", comment faire ?
Combien de groupes un user peut-il avoir ?
Quelle commande pour ajouter r2d2 dans le groupe secondaire "droid" ?
Je fais confiance à mon ami.e "Camille", je veux qu'il/elle ait les droits d'admin. Comment faire ?
Anakin en a marre de se connecter en voyant "anakin". Il vous contacte pour que ce soit changé en "Dark Vador". Comment faire ?
Sasuke étant devenu darksasuke, Naruto ne veut plus le voir dans le groupe "amis". Quelle commande taper ?
Le Diable est venu en personne me demander de lui créer l'utilisateur "godsuxx" avec une ID de 666 ? Quelle commande taper ?
En tant que bon admin, quelles sont les deux commandes "soeurs" concernant les droits sur les fichiers ?
Si je vous dis 755 ou 740... A quoi cela vous fait-il penser ? Peux-tu donner le nom exact ?
Dans   "  -rwxr-x---   "  que signifie le premier tiret ?
Quelle commande est l'équivalent du "Gestionnaire des tâches de Windows" ?
Comment afficher tous les processus lancés par un utilisateur ?
Qu'est-ce qu'un TTY ? Comment en changer ?
Qu'est-ce qu'un job ?
Comment lancer un processus en arrière-plan ? Comment le remettre au premier plan ?
Quelle commande permet d'envoyer un signal à un processus ?
Quelle commande permet de forcer l'arrêt d'un processus en cours ?
Par quoi sont gérés les services sur nos VM ?
Comment voir la liste de tous les services ?
Comment faire en sorte qu'un service se lance au démarrage de mon OS ?
Quelle commande permet de voir tous les logs de tout ce qui s'est passé depuis le premier boot de l'OS sur une machine ?
J'ai un problème avec un service en particulier, quelle commande permet d'avoir plus d'informations ?
Que signifie le terme "hasher" ? Quel est l'intérêt ? (si tu bloques, demande à Antton il expliquera sûrement ça très bien)
Quelle commande pour créer un répertoire ?
Quelle commande pour créer un fichier ?
Quelle commande pour se déplacer dans son home directory ?
Quelle commande pour copier un fichier / répertoire ?
Quelle commande pour afficher TOUT ce que contient un répertoire ?
Quelle commande pour afficher le contenu d'un fichier dans le terminal ?
Quelle commande pour supprimer un fichier / répertoire ?
Quelle commande pour déplacer/renommer un fichier / répertoire ?
Quelle commande pour n'afficher que les 5 premières lignes d'un fichier ?
Quelle commande pour n'afficher que les 3 dernières lignes d'un fichier ?
Quelle commande pour afficher un message dans le terminal ?
Quelle commande pour n'afficher que le 3eme champ dans un fichier CSV ?
Quelle commande pour trier le contenu d'un fichier par ordre alphabétique ?
Quelle commande pour afficher uniquement des lignes contenant un pattern bien précis ?
Quel est le raccourci signifiant " dossier parent " ?
Quel est le raccourci signifiant " dossier actuel " ?
Quelle commande pour savoir dans quel directory on se trouve ?
Il existe deux types de chemins, lesquels ? Explique nous la différence.
Comment afficher les fichiers cachés dans un dossier ?
Avec Linux " Tout est un fichier " , peux-tu citer un maximum de types de fichiers ?
Quelle commande pour afficher une belle arborescence dans le terminal ?
Comment n'afficher que la 7eme ligne d'un fichier ? (en 1 ou 2 commandes)
Au fait, c'est quoi " / " ?
A quoi sert " | " ?
Comment ne pas afficher la sortie d'erreur d'une commande dans le terminal ?
Je veux écrire "MERCI A TOUS" à la fin de mon fichier sans l'ouvrir, comment faire ?
Quelle commande permet d'avoir des infos sur un utilisateur (son uid, son groupe principal et tous ses groupes secondaires) ?
Comment lire le contenu d'un fichier interactivement ? (sans utiliser d'éditeur de texte)
Quelle touche magique permet de taper à la vitesse de l'éclair ?
J'ai un fichier avec des noms, et un autre avec les numéros de téléphone correspondants, comment regrouper ces infos dans un même fichier ?
Quelle commande permet d'afficher tout ce qui est branché physiquement à la machine ?
Où sont répertoriés tous les disques et périphériques ? (quel dossier)
Concrètement, que fait la commande 'dd' ?
Avec quelle commande peut-on nettoyer un disque entièrement ?
Je veux nettoyer UNIQUEMENT ma table de partitions, comment faire ?
A quoi sert cfdisk ?
Avant de monter une partition, je dois m'assurer que ... ?
Quelle est la trinité des étapes de création d'une partition avec LVM ?
Comment augmenter la taille d'une partition pour qu'elle prenne tout l'espace disponible ?
Quelle est la commande pour chiffrer une partition ?
Quelle commande permet de visualiser la taille réelle de mon home directory ? (l'espace disque utilisé)
Comment créer une archive compressée d'un fichier ? Par convention, comment la nomme-t-on ?
Quelle commande permet d'afficher tout ce qui est monté sur ma machine, avec l'espace utilisé et disponible ?
Comment rendre toutes les partitions d'un groupe inactives ? (très pratique quand on veut les supprimer)
Quelle est l'ip associée à "localhost" ?
Dans quel fichier peut-on associer des noms de domaines à des ip ?
Quelle commande permet d'afficher toutes nos interfaces de connexion ?
Quelle commande pour visualiser tout ce qui "écoute" sur notre machine ?
Quelle est l'ip associée à " toutes nos interfaces d'écoute " ?
Quel est le format d'une adresse IPv4 ? et IPv6 ?
Qu'est-ce qu'une adresse MAC ?
Quel est le format d'une adresse MAC ?
Quelle commande permet de tester rapidement si une machine est joignable ?
Quelle manip' a-t-on effectué pour permettre à toutes nos VM (de m2i) d'être sur le même réseau ?
Quelle est la différence entre une IP locale et une IP publique ? Comment sont-elles attribuées ?
Quel logiciel est utilisé pour voir tout ce qui se passe dans les différentes couches du modele OSI ? (utilisé par les H@CK3RZZ)
Quelle commande permet de scanner tous les ports d'une machine, de savoir ceux qui sont ouverts, et ce qu'il s'y passe ?
Quelle commande permet d'afficher le trajet des paquets vers une IP ?
Dans la pratique, que permet une redirection de port ? Quelle est la règle utilisée ?
Par quelle ligne doit absolument commencer un script ?
Pour commenter une ligne dans un script, quel symbole dois-je mettre ? Et si je veux commenter toute une partie ?
Qu'est-ce que '$@' dans un script ?
A quoi sert ${BASH_REMATCH} ? (non ce n'est pas rejouer le match...)
A quoi correspond la RegEx   [aeiouy]{1,3} ?
Dans une RegEx, on a souvent besoin de préciser début et fin de ligne, quels sont les symboles correspondants ?
Dans une RegEx, je veux matcher avec "le dernier mot d'une question" , comment faire ?
Ecris une RegEx pour matcher avec une IP. (pas de chance)
Je ne sais pas si tu te souviens, mais il y avait une histoire de mode non-glouton ... Tu peux nous rappeler ce que c'est ?
Dans un script bash, je veux faire une opération numérique, comment l'écrire ?
Comment récupérer une variable que l'utilisateur du script doit taper ? (prompt)
Je veux comparer deux valeurs numériques dans un script. Je peux utiliser  " > = < != "  ?
Comment générer un nombre aléatoire entre 1 et 100 ?
Qu'est-ce que l'IFS ? (Ce n'est pas l'Impot Fûr la Sortune...)
Comment récuperer le code de sortie d'une commande ? (utile pour vérifier si une commande fonctionne)
Pour pouvoir éxécuter mon script depuis n'importe où sur mon système, quelle variable d'environnement dois-je modifier ?
Très utile dans un script, comment faire une boucle " pour i allant de 1 à 10 " ?
Cite au moins une chose que Loup n'aime pas dans un script. Fais-toi plaisir.
Dans un script, comment envoyer une chaîne de commandes à effectuer dans une autre commande ? (vu dans le script sentinelle)
Dans un script, comment traduire (en bash) " Si 'mon nombre' est compris entre 10 et 23 " ?
Comment forcer l'utilisation d'un script en tant que sudoer ? (obligé de taper sudo bash script.sh pour exécuter le script)
J'ai beaucoup de variables à déclarer, et je ne veux pas que cela pollue mon script, comment faire pour rendre le tout plus propre ?
Sur nginx, comment activer un site déjà existant ? Il y a une alternative avec Apache, laquelle ?
Quelle extension ne pas oublier de préciser lorsque je nomme mon fichier de configuration d'un serveur avec Apache ?
A quoi sert docker ?
Quelle est la différence entre une connexion SFTP et une connexion FTPS ?
Quel protocole de sécurisation est utilisé dans une connexion HTTPS ?
Si je veux configurer un serveur de type "https", de quoi ai-je absolument besoin ?
Quelle est la commande pour générer un certificat et une clé de chiffrement associée ?
Par défaut, quel port est utilisé par un serveur nginx/apache ?
Par défaut, quel port est utilisé par un serveur nginx/apache sécurisé ?
Par défaut, quel port est utilisé par un serveur FTP ?
Par défaut, quel port est utilisé lors de la création d'un serveur Minecraft ? (mais si, vous savez, celui du voisin de Loup)
Par défaut, quel port est utilisé par un serveur Tomcat ?
Partage ton écran, et dessine nous ton meilleur schéma représentant ta VM dans le réseau m2i ? (avec des superbes rectangles)
Quel est l'intérêt des volumes avec Docker ?
Je veux proposer une nouvelle version de mon dépôt Git, quelles sont les 3 commandes à faire ?
A quoi sert GitHub ?
Décris nous la syntaxe de base d'un fichier de conf serveur (basique, simple) avec nginx.
Décris nous la syntaxe de base d'un fichier de conf serveur (basique, simple) avec apache.
Quel protocole est utilisé pour remplacer HTTP dans un serveur Tomcat ?
Décris nous la syntaxe de base d'un fichier de conf serveur SECURISE avec nginx.
Décris nous la syntaxe de base d'un fichier de conf serveur SECURISE avec apache.
Je veux répartir les clients de mon site https, sur mes serveurs de back1 et back2, comment faire ?
Je veux forcer la connexion protégée (https) lorsqu'un utilisateur se connecte à mon site, comment faire ?
Quel logiciel on utilise pour envoyer des fichiers sur un serveur FTP ?
Dans les directives des fichiers de conf serveur, que permet nginx que ne permet pas Apache ? (et c'est bien dommage...)
Tu préfères apache ou nginx ? Explique nous pourquoi ? (parce que c'est plus simple n'est pas toléré !)
Après une modification dans un fichier de conf serveur, que faut-il faire pour qu'elle soit prise en compte ? (commande)
Quelle est la commande pour obtenir une paire de clés utiles pour une connexion SSH ? Où sont-elles créées par défaut ?
Dans VIM, comment insérer du texte ?
Dans VIM, quelle est la méthode la plus rapide pour commenter rapidement tout un bloc ?
Dans VIM, comment quitter sans sauvegarder ?
Dans VIM, comment enregistrer les modifications ?
Dans VIM, comment supprimer une ligne rapidement ?
Dans VIM, comment se placer en fin de ligne directement ?
Dans VIM, comment annuler une modification ? Comment y revenir ?
J'ai oublié de lancer VIM en sudo, comment je peux quand même enregistrer mes modifications sans quitter et tout refaire ?
Comment chercher un pattern dans mon fichier vim ?
Dans un fichier que j'ai ouvert avec VIM, il y a 250 fois le mot "Yoda". Comment remplacer "Yoda" par "Luke" ?
J'ai créé un index.html, mais mon affichage n'est pas très beau... Comment faire pour afficher mon contenu sous forme d'une jolie liste ?
